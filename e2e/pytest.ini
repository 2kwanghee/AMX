[pytest]
# Own rootdir so the suite behaves the same whether it is invoked from the repo
# root or from this directory. `conftest` is imported by name from the test
# module, which needs this directory on sys.path.
testpaths = .
