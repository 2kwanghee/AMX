// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: amx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "amx.v1";

/**
 * AMX — Account Management & eXchange
 * AMS <-> AMA control-plane contract.
 *
 * SSOT: docs/AMX-DESIGN.md (§5.2 assignment state machine, §5.4 transport,
 * §6.2 local store / crypto, §6.3 command table, §6.5 report schema).
 * Contract-first: this file changes before any implementation.
 */

/**
 * Credential kind held by an account (§5.1 accounts.credential_type).
 * setup_token is deliberately absent — it is retired by D3 / §2.4-5.
 */
export enum CredentialType {
  CREDENTIAL_TYPE_UNSPECIFIED = 0,
  /** CREDENTIAL_TYPE_OAUTH - full claude login credential set (§5.5) */
  CREDENTIAL_TYPE_OAUTH = 1,
  /** CREDENTIAL_TYPE_API_KEY - O3 — no subscription quota, 95% threshold is moot */
  CREDENTIAL_TYPE_API_KEY = 2,
  UNRECOGNIZED = -1,
}

export function credentialTypeFromJSON(object: any): CredentialType {
  switch (object) {
    case 0:
    case "CREDENTIAL_TYPE_UNSPECIFIED":
      return CredentialType.CREDENTIAL_TYPE_UNSPECIFIED;
    case 1:
    case "CREDENTIAL_TYPE_OAUTH":
      return CredentialType.CREDENTIAL_TYPE_OAUTH;
    case 2:
    case "CREDENTIAL_TYPE_API_KEY":
      return CredentialType.CREDENTIAL_TYPE_API_KEY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CredentialType.UNRECOGNIZED;
  }
}

export function credentialTypeToJSON(object: CredentialType): string {
  switch (object) {
    case CredentialType.CREDENTIAL_TYPE_UNSPECIFIED:
      return "CREDENTIAL_TYPE_UNSPECIFIED";
    case CredentialType.CREDENTIAL_TYPE_OAUTH:
      return "CREDENTIAL_TYPE_OAUTH";
    case CredentialType.CREDENTIAL_TYPE_API_KEY:
      return "CREDENTIAL_TYPE_API_KEY";
    case CredentialType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * AMS-authoritative allocation status of one account on one agent.
 * Projection of §5.2 assignment states onto what AMA can observe locally.
 */
export enum AllocationStatus {
  ALLOCATION_STATUS_UNSPECIFIED = 0,
  /** ALLOCATION_STATUS_DELIVERING - credential write + `tsamx add` in flight */
  ALLOCATION_STATUS_DELIVERING = 1,
  /** ALLOCATION_STATUS_ACTIVE - `tsamx enable` — rotation candidate */
  ALLOCATION_STATUS_ACTIVE = 2,
  /** ALLOCATION_STATUS_INACTIVE - `tsamx disable` — credential kept, rotation excluded */
  ALLOCATION_STATUS_INACTIVE = 3,
  /** ALLOCATION_STATUS_QUARANTINED - tsamx quarantine (exhausted / repeated failure) */
  ALLOCATION_STATUS_QUARANTINED = 4,
  /** ALLOCATION_STATUS_RECALLING - `tsamx remove` in flight */
  ALLOCATION_STATUS_RECALLING = 5,
  /** ALLOCATION_STATUS_ABSENT - not present locally (detached, or never delivered) */
  ALLOCATION_STATUS_ABSENT = 6,
  UNRECOGNIZED = -1,
}

export function allocationStatusFromJSON(object: any): AllocationStatus {
  switch (object) {
    case 0:
    case "ALLOCATION_STATUS_UNSPECIFIED":
      return AllocationStatus.ALLOCATION_STATUS_UNSPECIFIED;
    case 1:
    case "ALLOCATION_STATUS_DELIVERING":
      return AllocationStatus.ALLOCATION_STATUS_DELIVERING;
    case 2:
    case "ALLOCATION_STATUS_ACTIVE":
      return AllocationStatus.ALLOCATION_STATUS_ACTIVE;
    case 3:
    case "ALLOCATION_STATUS_INACTIVE":
      return AllocationStatus.ALLOCATION_STATUS_INACTIVE;
    case 4:
    case "ALLOCATION_STATUS_QUARANTINED":
      return AllocationStatus.ALLOCATION_STATUS_QUARANTINED;
    case 5:
    case "ALLOCATION_STATUS_RECALLING":
      return AllocationStatus.ALLOCATION_STATUS_RECALLING;
    case 6:
    case "ALLOCATION_STATUS_ABSENT":
      return AllocationStatus.ALLOCATION_STATUS_ABSENT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AllocationStatus.UNRECOGNIZED;
  }
}

export function allocationStatusToJSON(object: AllocationStatus): string {
  switch (object) {
    case AllocationStatus.ALLOCATION_STATUS_UNSPECIFIED:
      return "ALLOCATION_STATUS_UNSPECIFIED";
    case AllocationStatus.ALLOCATION_STATUS_DELIVERING:
      return "ALLOCATION_STATUS_DELIVERING";
    case AllocationStatus.ALLOCATION_STATUS_ACTIVE:
      return "ALLOCATION_STATUS_ACTIVE";
    case AllocationStatus.ALLOCATION_STATUS_INACTIVE:
      return "ALLOCATION_STATUS_INACTIVE";
    case AllocationStatus.ALLOCATION_STATUS_QUARANTINED:
      return "ALLOCATION_STATUS_QUARANTINED";
    case AllocationStatus.ALLOCATION_STATUS_RECALLING:
      return "ALLOCATION_STATUS_RECALLING";
    case AllocationStatus.ALLOCATION_STATUS_ABSENT:
      return "ALLOCATION_STATUS_ABSENT";
    case AllocationStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Server-scoped switching mode (§5.2 — mode is a server property because
 * `tsamx auto` operates on the whole local pool, not per account).
 */
export enum SwitchMode {
  SWITCH_MODE_UNSPECIFIED = 0,
  /** SWITCH_MODE_AUTO - scheduler runs `tsamx auto --once` ticks (D8) */
  SWITCH_MODE_AUTO = 1,
  /** SWITCH_MODE_MANUAL - ticks stopped; switching only on explicit SwitchNow */
  SWITCH_MODE_MANUAL = 2,
  UNRECOGNIZED = -1,
}

export function switchModeFromJSON(object: any): SwitchMode {
  switch (object) {
    case 0:
    case "SWITCH_MODE_UNSPECIFIED":
      return SwitchMode.SWITCH_MODE_UNSPECIFIED;
    case 1:
    case "SWITCH_MODE_AUTO":
      return SwitchMode.SWITCH_MODE_AUTO;
    case 2:
    case "SWITCH_MODE_MANUAL":
      return SwitchMode.SWITCH_MODE_MANUAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SwitchMode.UNRECOGNIZED;
  }
}

export function switchModeToJSON(object: SwitchMode): string {
  switch (object) {
    case SwitchMode.SWITCH_MODE_UNSPECIFIED:
      return "SWITCH_MODE_UNSPECIFIED";
    case SwitchMode.SWITCH_MODE_AUTO:
      return "SWITCH_MODE_AUTO";
    case SwitchMode.SWITCH_MODE_MANUAL:
      return "SWITCH_MODE_MANUAL";
    case SwitchMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Symmetric AEAD used for the credential envelope (§6.2). */
export enum EncryptionAlgorithm {
  ENCRYPTION_ALGORITHM_UNSPECIFIED = 0,
  ENCRYPTION_ALGORITHM_AES_256_GCM = 1,
  UNRECOGNIZED = -1,
}

export function encryptionAlgorithmFromJSON(object: any): EncryptionAlgorithm {
  switch (object) {
    case 0:
    case "ENCRYPTION_ALGORITHM_UNSPECIFIED":
      return EncryptionAlgorithm.ENCRYPTION_ALGORITHM_UNSPECIFIED;
    case 1:
    case "ENCRYPTION_ALGORITHM_AES_256_GCM":
      return EncryptionAlgorithm.ENCRYPTION_ALGORITHM_AES_256_GCM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EncryptionAlgorithm.UNRECOGNIZED;
  }
}

export function encryptionAlgorithmToJSON(object: EncryptionAlgorithm): string {
  switch (object) {
    case EncryptionAlgorithm.ENCRYPTION_ALGORITHM_UNSPECIFIED:
      return "ENCRYPTION_ALGORITHM_UNSPECIFIED";
    case EncryptionAlgorithm.ENCRYPTION_ALGORITHM_AES_256_GCM:
      return "ENCRYPTION_ALGORITHM_AES_256_GCM";
    case EncryptionAlgorithm.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Stable identity of an account across AMS and AMA.
 * The mapping key is email/uuid, never a tsamx slot number (§2.4-3).
 */
export interface AccountRef {
  /** AMS accounts.id (UUID string) */
  amsAccountId: string;
  /** tsamx mapping key */
  email: string;
  /** Claude account UUID from the credential metadata (optional) */
  accountUuid: string;
}

/**
 * AES-256-GCM envelope carrying a complete OAuth credential set (D3, §5.5):
 * accessToken + refreshToken + expiresAt + full scopes + account/org metadata.
 *
 * Plaintext is the credential-set JSON exactly as `claude login` would produce,
 * so the receiving box's Claude Code cannot distinguish it from a real login.
 *
 * Key: the KEK is handed to AMA by SessionSetup and held in memory only
 * (§6.2, open item O1) — an off-box copy of the manifest cannot be decrypted.
 * `key_id` selects which delivered KEK to use.
 *
 * AAD binding (§6.2): the additional authenticated data is the canonical
 * concatenation of (ams_account_id, agent_id) of the intended recipient. This
 * prevents a record from being copied or swapped into another agent's manifest:
 * decryption on the wrong agent fails authentication rather than yielding data.
 *
 * ⚠ The aad_* fields below are for COMPARISON ONLY. AMA MUST construct the AAD
 * from values it already holds — its own agent_id, and the ams_account_id of the
 * command being processed — and MUST NOT feed these received field values into
 * the AEAD. Using the attacker-supplied copy as AAD would let a relocated record
 * authenticate anywhere, defeating the binding. Treat a mismatch between the
 * received aad_* values and the locally derived ones as a rejected command.
 *
 * Never log ciphertext, nonce, or plaintext (§7 in-transit row).
 */
export interface EncryptedCredential {
  algorithm: EncryptionAlgorithm;
  /** AEAD output, includes the GCM tag */
  ciphertext: Uint8Array;
  /** 96-bit, unique per record under a given key */
  nonce: Uint8Array;
  /** selects a KEK delivered by SessionSetup */
  keyId: string;
  /**
   * Cross-check copies. MUST equal the locally derived AAD components; never
   * used as the AAD input itself (see the warning above).
   */
  aadAmsAccountId: string;
  /** expected == this agent's own agent_id */
  aadAgentId: string;
}

/** One binding window's utilization (§2.2 — max of 5h / 7d is the switch driver). */
export interface UsageWindow {
  /** 0.0 – 100.0 */
  pct: number;
  resetsAt: Date | undefined;
}

export interface AccountUsage {
  account: AccountRef | undefined;
  allocationStatus: AllocationStatus;
  /** holds ~/.claude/.credentials.json right now (D2 single active) */
  isCurrent: boolean;
  fiveHour: UsageWindow | undefined;
  sevenDay:
    | UsageWindow
    | undefined;
  /** freshness of the tsamx cache entry */
  usageFetchedAt: Date | undefined;
}

/** Aggregate pool health (§6.5 poolSummary). */
export interface PoolSummary {
  total: number;
  active: number;
  /** active and below threshold, i.e. switch candidates */
  eligible: number;
  quarantined: number;
  /** every account >= threshold — critical, AMS must react (§6.4) */
  allExhausted: boolean;
  maxUtilizationPct: number;
}

/**
 * Ack for the unary ReportUsage fallback. Transport-level receipt only;
 * command convergence is reported via CommandAck.
 */
export interface Ack {
  accepted: boolean;
  message: string;
  receivedAt: Date | undefined;
}

export interface AmsCommand {
  /**
   * Idempotency key. AMA keeps a processed-command log; a repeat MUST be a no-op
   * that re-emits the current convergence state rather than re-running the effect.
   */
  commandId: string;
  /**
   * Ed25519 signature by AMS over the canonical serialization of this message
   * with `signature` cleared. AMA verifies with the public key baked into its
   * build and executes nothing that fails verification (§3 trust model, §6.2:
   * authority is enforced by signature, not by encryption).
   */
  signature: Uint8Array;
  /**
   * Issue time, carried inside the signed payload so AMA can reject stale
   * replays outside its acceptance window.
   */
  issuedAt:
    | Date
    | undefined;
  /**
   * Intended recipient. AMS fills this with the authenticated agent_id of the
   * session it is writing to, and — being part of this message — it is covered
   * by `signature`. AMA MUST reject any command whose target_agent_id is not its
   * own AMX_AGENT_ID, so a command signed for one agent cannot be re-injected
   * into another (cross-agent / cross-tenant replay). SessionSetup included.
   */
  targetAgentId: string;
  deliver?: DeliverAccount | undefined;
  recall?: RecallAccount | undefined;
  setActive?: SetAccountActive | undefined;
  setMode?: SetSwitchMode | undefined;
  switchNow?: SwitchNow | undefined;
  reqReport?: RequestReport | undefined;
  sessionSetup?: SessionSetup | undefined;
}

/**
 * Session establishment layer, sent by AMS at the start of a session and again
 * whenever material must be rotated. It carries two things:
 *
 *   1. Enrollment promotion — when the agent registered with a one-shot
 *      enroll_token, AMS issues the long-lived server_credential here. The agent
 *      persists it and presents it on every later Register (§7 AMA 인증).
 *   2. KEK delivery (§6.2) — the key-encryption keys that open manifest records
 *      and DeliverAccount envelopes. Held in memory only, so they must be
 *      re-delivered after every agent restart; that is what makes local account
 *      data unreadable without a live AMS session.
 *
 * This is an AmsCommand like any other, so it is covered by the Ed25519
 * signature in AmsCommand.signature and MUST be rejected if verification fails.
 * An agent that cannot verify it has no keys and therefore no working manifest.
 */
export interface SessionSetup {
  /**
   * Set only on the enroll -> enrolled promotion; empty on ordinary sessions.
   * Replaces any credential the agent currently holds.
   */
  serverCredential: string;
  /**
   * One or more KEKs. Multiple entries let a rotation overlap: records written
   * under the old key_id stay readable while new records use the new one.
   * An empty list means "keep the keys you already have".
   */
  keys: SessionSetup_WrappedKey[];
  /**
   * The key_id AMA should use when it needs to write a new record itself.
   * Must name one of `keys`, or a key already held.
   */
  activeKeyId: string;
  /** key_ids AMA must drop from memory now (completed rotation). */
  revokedKeyIds: string[];
}

export interface SessionSetup_WrappedKey {
  keyId: string;
  /**
   * The KEK, wrapped for this agent in transit. Plaintext KEK material never
   * touches disk on the agent (§6.2) and is never logged (§7).
   */
  wrappedKey: Uint8Array;
  algorithm: EncryptionAlgorithm;
  /** advisory expiry, for rotation planning */
  notAfter: Date | undefined;
}

/**
 * Push a complete credential set to the agent (D3).
 *
 * slot 없음 — AMA 로컬 자동 할당, 매핑 키 email/uuid.
 * There is intentionally NO slot field: `tsamx add` assigns the slot number
 * locally and slot numbers shift on `remove` (§2.4-3), so AMS never names one.
 *
 * Local procedure (§6.3 deliver, single critical section):
 *   verify signature -> decrypt credential set -> manifest upsert
 *   -> record previously active account -> write ~/.claude/.credentials.json
 *      and ~/.claude.json oauthAccount -> `tsamx add` -> switch back to the
 *      previously active account if needed -> wipe plaintext from memory.
 */
export interface DeliverAccount {
  /** AMS assignments.id — ack correlates back to this row */
  assignmentId: string;
  account: AccountRef | undefined;
  credentialType: CredentialType;
  encryptedCredential:
    | EncryptedCredential
    | undefined;
  /**
   * Allocation the agent should converge to once the credential is installed.
   * ACTIVE -> `tsamx enable`, INACTIVE -> `tsamx disable`.
   *
   * This is the only rotation control on the wire. `assignments.pinned` (§5.2)
   * is an AMS-internal scheduling flag and is deliberately NOT sent: AMS
   * translates a pinned assignment into desired_status = INACTIVE before issuing
   * the command. Shipping both would give the agent two sources of truth for one
   * decision and a convergence loop when they disagree.
   */
  desiredStatus: AllocationStatus;
  /**
   * Credential-set metadata AMS already holds, echoed so AMA can populate
   * ~/.claude.json oauthAccount without decrypting for display purposes.
   */
  organizationName: string;
  credentialExpiresAt: Date | undefined;
}

/**
 * Remove an account from the agent (§6.3 recall).
 * If the target is currently active, AMA switches away first, then removes.
 */
export interface RecallAccount {
  assignmentId: string;
  account:
    | AccountRef
    | undefined;
  /**
   * O2 (open item): true = delete the local credential copy entirely (current
   * design), false = keep the manifest record and only disable it for fast
   * re-assignment. AMS decides; AMA obeys.
   */
  purgeLocalCopy: boolean;
}

/**
 * activate / deactivate (§6.3). Also the mechanism behind REST `:recover`
 * (§5.2 — quarantined -> active is driven by set_active(activate)).
 */
export interface SetAccountActive {
  assignmentId: string;
  account:
    | AccountRef
    | undefined;
  /** true -> `tsamx enable`, false -> `tsamx disable` */
  active: boolean;
  /** recover path: also clear tsamx quarantine for this account */
  clearQuarantine: boolean;
}

/**
 * Server-scoped auto/manual switch (§5.2 — non-state command, does not advance
 * any assignment state; it only changes servers.switch_mode).
 */
export interface SetSwitchMode {
  mode: SwitchMode;
}

/** Manual switch (§6.3 switch_now). Non-state command: only last_switched_at moves. */
export interface SwitchNow {
  /** Switch to this specific account: `tsamx switch <email>`. */
  account?:
    | AccountRef
    | undefined;
  /** Let tsamx rank candidates: `tsamx switch --strategy best`. */
  strategy?:
    | SwitchNow_SwitchStrategy
    | undefined;
  /**
   * Assignment the request originated from, for correlation and the audit trail.
   * Symmetric with the other account-scoped commands. Empty when `strategy` is
   * used, since no single assignment is named.
   */
  assignmentId: string;
}

export enum SwitchNow_SwitchStrategy {
  SWITCH_STRATEGY_UNSPECIFIED = 0,
  SWITCH_STRATEGY_BEST = 1,
  SWITCH_STRATEGY_NEXT_AVAILABLE = 2,
  UNRECOGNIZED = -1,
}

export function switchNow_SwitchStrategyFromJSON(object: any): SwitchNow_SwitchStrategy {
  switch (object) {
    case 0:
    case "SWITCH_STRATEGY_UNSPECIFIED":
      return SwitchNow_SwitchStrategy.SWITCH_STRATEGY_UNSPECIFIED;
    case 1:
    case "SWITCH_STRATEGY_BEST":
      return SwitchNow_SwitchStrategy.SWITCH_STRATEGY_BEST;
    case 2:
    case "SWITCH_STRATEGY_NEXT_AVAILABLE":
      return SwitchNow_SwitchStrategy.SWITCH_STRATEGY_NEXT_AVAILABLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SwitchNow_SwitchStrategy.UNRECOGNIZED;
  }
}

export function switchNow_SwitchStrategyToJSON(object: SwitchNow_SwitchStrategy): string {
  switch (object) {
    case SwitchNow_SwitchStrategy.SWITCH_STRATEGY_UNSPECIFIED:
      return "SWITCH_STRATEGY_UNSPECIFIED";
    case SwitchNow_SwitchStrategy.SWITCH_STRATEGY_BEST:
      return "SWITCH_STRATEGY_BEST";
    case SwitchNow_SwitchStrategy.SWITCH_STRATEGY_NEXT_AVAILABLE:
      return "SWITCH_STRATEGY_NEXT_AVAILABLE";
    case SwitchNow_SwitchStrategy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Ask for an immediate report (§6.3 req_report, REST `:refresh-usage`).
 * Always executed — reading is idempotent.
 */
export interface RequestReport {
  reportType: RequestReport_ReportType;
  /** free-form, for the audit trail (e.g. "console refresh") */
  reason: string;
}

export enum RequestReport_ReportType {
  REPORT_TYPE_UNSPECIFIED = 0,
  REPORT_TYPE_USAGE = 1,
  UNRECOGNIZED = -1,
}

export function requestReport_ReportTypeFromJSON(object: any): RequestReport_ReportType {
  switch (object) {
    case 0:
    case "REPORT_TYPE_UNSPECIFIED":
      return RequestReport_ReportType.REPORT_TYPE_UNSPECIFIED;
    case 1:
    case "REPORT_TYPE_USAGE":
      return RequestReport_ReportType.REPORT_TYPE_USAGE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RequestReport_ReportType.UNRECOGNIZED;
  }
}

export function requestReport_ReportTypeToJSON(object: RequestReport_ReportType): string {
  switch (object) {
    case RequestReport_ReportType.REPORT_TYPE_UNSPECIFIED:
      return "REPORT_TYPE_UNSPECIFIED";
    case RequestReport_ReportType.REPORT_TYPE_USAGE:
      return "REPORT_TYPE_USAGE";
    case RequestReport_ReportType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface AmaMessage {
  register?: Register | undefined;
  hb?: Heartbeat | undefined;
  usage?: UsageReport | undefined;
  ack?: CommandAck | undefined;
  event?: AccountEvent | undefined;
}

/**
 * First message on every stream. Presents the agent's credential (§7 AMA 인증):
 * one-shot enroll_token on first contact, long-lived server_credential after.
 * AMS binds the session to the tenant recorded for that server — a tenant id
 * supplied by the client is never trusted (§7 tenant isolation, triple defense).
 */
export interface Register {
  /** stable agent identifier, "ama_..." */
  agentId: string;
  /** AMS servers.id, known after enrollment */
  serverId: string;
  /** one-shot, hash-only on the AMS side */
  enrollToken?:
    | string
    | undefined;
  /** long-lived, exchanged during enrollment */
  serverCredential?: string | undefined;
  hostname: string;
  agentVersion: string;
  tsamxVersion: string;
  /**
   * Local reality at connect time, so AMS can reconcile immediately (§5.4)
   * instead of waiting for the first scheduled report.
   */
  switchMode: SwitchMode;
  accounts: AccountUsage[];
  /**
   * Command ids AMA has already applied, so AMS can suppress redundant redelivery
   * after a reconnect. Bounded: the 128 most recent ids, newest first. It is a
   * reconnect optimization, not an authoritative log — every command is
   * idempotent anyway (§6.3), so a redelivery beyond the window is harmless.
   */
  appliedCommandIds: string[];
}

export interface Heartbeat {
  agentId: string;
  sentAt: Date | undefined;
  activeAccount: AccountRef | undefined;
  switchMode: SwitchMode;
  tsamxHealthy: boolean;
  /** queued events pending flush (§6.3 offline outbox) */
  outboxDepth: number;
}

/**
 * §6.5 usage report — schedule (5 min), ams_query (RequestReport), or switch.
 * AMA does NOT poll the usage API itself; it re-serializes the tsamx cache
 * exposed by `tsamx list --json` (avoids double polling / rate limits).
 */
export interface UsageReport {
  /** == 1, matches contracts/schemas/usage-report.schema.json */
  schemaVersion: number;
  agentId: string;
  generatedAt: Date | undefined;
  trigger: UsageReport_Trigger;
  activeAccount: AccountRef | undefined;
  poolSummary: PoolSummary | undefined;
  accounts: AccountUsage[];
  /** Set when trigger == AMS_QUERY, correlating back to the RequestReport. */
  inResponseToCommandId: string;
}

export enum UsageReport_Trigger {
  TRIGGER_UNSPECIFIED = 0,
  /** TRIGGER_SCHEDULE - 5-minute poll */
  TRIGGER_SCHEDULE = 1,
  /** TRIGGER_AMS_QUERY - RequestReport / REST :refresh-usage */
  TRIGGER_AMS_QUERY = 2,
  /** TRIGGER_SWITCH - emitted alongside a switch event */
  TRIGGER_SWITCH = 3,
  UNRECOGNIZED = -1,
}

export function usageReport_TriggerFromJSON(object: any): UsageReport_Trigger {
  switch (object) {
    case 0:
    case "TRIGGER_UNSPECIFIED":
      return UsageReport_Trigger.TRIGGER_UNSPECIFIED;
    case 1:
    case "TRIGGER_SCHEDULE":
      return UsageReport_Trigger.TRIGGER_SCHEDULE;
    case 2:
    case "TRIGGER_AMS_QUERY":
      return UsageReport_Trigger.TRIGGER_AMS_QUERY;
    case 3:
    case "TRIGGER_SWITCH":
      return UsageReport_Trigger.TRIGGER_SWITCH;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UsageReport_Trigger.UNRECOGNIZED;
  }
}

export function usageReport_TriggerToJSON(object: UsageReport_Trigger): string {
  switch (object) {
    case UsageReport_Trigger.TRIGGER_UNSPECIFIED:
      return "TRIGGER_UNSPECIFIED";
    case UsageReport_Trigger.TRIGGER_SCHEDULE:
      return "TRIGGER_SCHEDULE";
    case UsageReport_Trigger.TRIGGER_AMS_QUERY:
      return "TRIGGER_AMS_QUERY";
    case UsageReport_Trigger.TRIGGER_SWITCH:
      return "TRIGGER_SWITCH";
    case UsageReport_Trigger.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Request wrapper for the unary ReportUsage fallback, which has no
 * authenticated stream to carry identity.
 *
 * The credential lives here and not inside UsageReport on purpose: AMS stores
 * the report verbatim in usage_snapshots.payload (§5.1), so anything embedded in
 * it is persisted and surfaced by `GET …/servers/{sid}/usage`. Keeping the
 * secret in the envelope means the stored payload is identical whether it
 * arrived over the stream or over this fallback.
 */
export interface ReportEnvelope {
  /**
   * Long-lived server credential, same value the agent presents in Register.
   * Authenticates the caller and binds the report to a tenant. Never logged (§7).
   */
  serverCredential: string;
  report: UsageReport | undefined;
}

/**
 * Command result as a CONVERGENCE STATE, not success/failure (§6.3).
 * AMA reports the local reality it converged to; that report is the input to the
 * AMS reconcile loop (§5.4 desired vs actual). A repeated command that was
 * already applied still returns CONVERGED — the effect is a no-op, the answer
 * is not.
 */
export interface CommandAck {
  commandId: string;
  agentId: string;
  observedAt: Date | undefined;
  convergence: CommandAck_Convergence;
  /**
   * Observed local state after processing. AMS compares this against the
   * assignment row; a mismatch is drift and triggers a corrective re-issue.
   */
  accountState:
    | AccountUsage
    | undefined;
  /** set for set_mode, and echoed otherwise */
  switchMode: SwitchMode;
  poolSummary:
    | PoolSummary
    | undefined;
  /**
   * Populated when convergence is PENDING, DIVERGED or REJECTED. Diagnostic
   * only — MUST NOT contain credential material (§7).
   */
  detail: string;
  errorCode: string;
}

export enum CommandAck_Convergence {
  CONVERGENCE_UNSPECIFIED = 0,
  /** CONVERGENCE_CONVERGED - Local state matches what the command asked for (including no-op repeats). */
  CONVERGENCE_CONVERGED = 1,
  /**
   * CONVERGENCE_PENDING - Accepted, still working (e.g. inside the deliver critical section).
   * A further ack follows.
   */
  CONVERGENCE_PENDING = 2,
  /**
   * CONVERGENCE_DIVERGED - Applied what it could; local state still differs from the request.
   * AMS reconcile decides whether to retry (§5.2 ack.fail -> pending).
   */
  CONVERGENCE_DIVERGED = 3,
  /**
   * CONVERGENCE_REJECTED - Not executed: signature verification failed, stale issued_at, unknown
   * command, or the account does not belong to this agent's tenant.
   */
  CONVERGENCE_REJECTED = 4,
  UNRECOGNIZED = -1,
}

export function commandAck_ConvergenceFromJSON(object: any): CommandAck_Convergence {
  switch (object) {
    case 0:
    case "CONVERGENCE_UNSPECIFIED":
      return CommandAck_Convergence.CONVERGENCE_UNSPECIFIED;
    case 1:
    case "CONVERGENCE_CONVERGED":
      return CommandAck_Convergence.CONVERGENCE_CONVERGED;
    case 2:
    case "CONVERGENCE_PENDING":
      return CommandAck_Convergence.CONVERGENCE_PENDING;
    case 3:
    case "CONVERGENCE_DIVERGED":
      return CommandAck_Convergence.CONVERGENCE_DIVERGED;
    case 4:
    case "CONVERGENCE_REJECTED":
      return CommandAck_Convergence.CONVERGENCE_REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CommandAck_Convergence.UNRECOGNIZED;
  }
}

export function commandAck_ConvergenceToJSON(object: CommandAck_Convergence): string {
  switch (object) {
    case CommandAck_Convergence.CONVERGENCE_UNSPECIFIED:
      return "CONVERGENCE_UNSPECIFIED";
    case CommandAck_Convergence.CONVERGENCE_CONVERGED:
      return "CONVERGENCE_CONVERGED";
    case CommandAck_Convergence.CONVERGENCE_PENDING:
      return "CONVERGENCE_PENDING";
    case CommandAck_Convergence.CONVERGENCE_DIVERGED:
      return "CONVERGENCE_DIVERGED";
    case CommandAck_Convergence.CONVERGENCE_REJECTED:
      return "CONVERGENCE_REJECTED";
    case CommandAck_Convergence.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * §6.5 switch_event — pushed immediately, independent of the 5-minute cadence.
 * Detected by comparing `tsamx status --json` around each `auto --once` tick and
 * by watching autoswitch_state.json (§6.4 dual detection).
 */
export interface AccountEvent {
  /** == 1, matches contracts/schemas/switch-event.schema.json */
  schemaVersion: number;
  agentId: string;
  /** outbox dedupe key — AMS MUST ignore a repeated event_id */
  eventId: string;
  occurredAt: Date | undefined;
  kind: AccountEvent_Kind;
  trigger: AccountEvent_Trigger;
  /** unset for the first activation and for all_exhausted */
  from:
    | AccountRef
    | undefined;
  /** unset for quarantine and all_exhausted */
  to: AccountRef | undefined;
  poolSummary: PoolSummary | undefined;
  detail: string;
}

export enum AccountEvent_Kind {
  KIND_UNSPECIFIED = 0,
  KIND_SWITCH = 1,
  KIND_QUARANTINE = 2,
  /** KIND_ALL_EXHAUSTED - critical — AMS alerts and considers extra assignment (§6.4) */
  KIND_ALL_EXHAUSTED = 3,
  UNRECOGNIZED = -1,
}

export function accountEvent_KindFromJSON(object: any): AccountEvent_Kind {
  switch (object) {
    case 0:
    case "KIND_UNSPECIFIED":
      return AccountEvent_Kind.KIND_UNSPECIFIED;
    case 1:
    case "KIND_SWITCH":
      return AccountEvent_Kind.KIND_SWITCH;
    case 2:
    case "KIND_QUARANTINE":
      return AccountEvent_Kind.KIND_QUARANTINE;
    case 3:
    case "KIND_ALL_EXHAUSTED":
      return AccountEvent_Kind.KIND_ALL_EXHAUSTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AccountEvent_Kind.UNRECOGNIZED;
  }
}

export function accountEvent_KindToJSON(object: AccountEvent_Kind): string {
  switch (object) {
    case AccountEvent_Kind.KIND_UNSPECIFIED:
      return "KIND_UNSPECIFIED";
    case AccountEvent_Kind.KIND_SWITCH:
      return "KIND_SWITCH";
    case AccountEvent_Kind.KIND_QUARANTINE:
      return "KIND_QUARANTINE";
    case AccountEvent_Kind.KIND_ALL_EXHAUSTED:
      return "KIND_ALL_EXHAUSTED";
    case AccountEvent_Kind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AccountEvent_Trigger {
  TRIGGER_UNSPECIFIED = 0,
  /** TRIGGER_AT_LIMIT - utilization crossed the 95% threshold (D10) */
  TRIGGER_AT_LIMIT = 1,
  /** TRIGGER_MANUAL - SwitchNow */
  TRIGGER_MANUAL = 2,
  /** TRIGGER_FAILOVER - previous account failed / was quarantined */
  TRIGGER_FAILOVER = 3,
  UNRECOGNIZED = -1,
}

export function accountEvent_TriggerFromJSON(object: any): AccountEvent_Trigger {
  switch (object) {
    case 0:
    case "TRIGGER_UNSPECIFIED":
      return AccountEvent_Trigger.TRIGGER_UNSPECIFIED;
    case 1:
    case "TRIGGER_AT_LIMIT":
      return AccountEvent_Trigger.TRIGGER_AT_LIMIT;
    case 2:
    case "TRIGGER_MANUAL":
      return AccountEvent_Trigger.TRIGGER_MANUAL;
    case 3:
    case "TRIGGER_FAILOVER":
      return AccountEvent_Trigger.TRIGGER_FAILOVER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AccountEvent_Trigger.UNRECOGNIZED;
  }
}

export function accountEvent_TriggerToJSON(object: AccountEvent_Trigger): string {
  switch (object) {
    case AccountEvent_Trigger.TRIGGER_UNSPECIFIED:
      return "TRIGGER_UNSPECIFIED";
    case AccountEvent_Trigger.TRIGGER_AT_LIMIT:
      return "TRIGGER_AT_LIMIT";
    case AccountEvent_Trigger.TRIGGER_MANUAL:
      return "TRIGGER_MANUAL";
    case AccountEvent_Trigger.TRIGGER_FAILOVER:
      return "TRIGGER_FAILOVER";
    case AccountEvent_Trigger.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseAccountRef(): AccountRef {
  return { amsAccountId: "", email: "", accountUuid: "" };
}

export const AccountRef: MessageFns<AccountRef> = {
  encode(message: AccountRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.amsAccountId !== "") {
      writer.uint32(10).string(message.amsAccountId);
    }
    if (message.email !== "") {
      writer.uint32(18).string(message.email);
    }
    if (message.accountUuid !== "") {
      writer.uint32(26).string(message.accountUuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AccountRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.amsAccountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accountUuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountRef {
    return {
      amsAccountId: isSet(object.amsAccountId)
        ? globalThis.String(object.amsAccountId)
        : isSet(object.ams_account_id)
        ? globalThis.String(object.ams_account_id)
        : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      accountUuid: isSet(object.accountUuid)
        ? globalThis.String(object.accountUuid)
        : isSet(object.account_uuid)
        ? globalThis.String(object.account_uuid)
        : "",
    };
  },

  toJSON(message: AccountRef): unknown {
    const obj: any = {};
    if (message.amsAccountId !== "") {
      obj.amsAccountId = message.amsAccountId;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.accountUuid !== "") {
      obj.accountUuid = message.accountUuid;
    }
    return obj;
  },

  create(base?: DeepPartial<AccountRef>): AccountRef {
    return AccountRef.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AccountRef>): AccountRef {
    const message = createBaseAccountRef();
    message.amsAccountId = object.amsAccountId ?? "";
    message.email = object.email ?? "";
    message.accountUuid = object.accountUuid ?? "";
    return message;
  },
};

function createBaseEncryptedCredential(): EncryptedCredential {
  return {
    algorithm: 0,
    ciphertext: new Uint8Array(0),
    nonce: new Uint8Array(0),
    keyId: "",
    aadAmsAccountId: "",
    aadAgentId: "",
  };
}

export const EncryptedCredential: MessageFns<EncryptedCredential> = {
  encode(message: EncryptedCredential, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.algorithm !== 0) {
      writer.uint32(8).int32(message.algorithm);
    }
    if (message.ciphertext.length !== 0) {
      writer.uint32(18).bytes(message.ciphertext);
    }
    if (message.nonce.length !== 0) {
      writer.uint32(26).bytes(message.nonce);
    }
    if (message.keyId !== "") {
      writer.uint32(34).string(message.keyId);
    }
    if (message.aadAmsAccountId !== "") {
      writer.uint32(42).string(message.aadAmsAccountId);
    }
    if (message.aadAgentId !== "") {
      writer.uint32(50).string(message.aadAgentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EncryptedCredential {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEncryptedCredential();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.algorithm = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ciphertext = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nonce = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.keyId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.aadAmsAccountId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.aadAgentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EncryptedCredential {
    return {
      algorithm: isSet(object.algorithm) ? encryptionAlgorithmFromJSON(object.algorithm) : 0,
      ciphertext: isSet(object.ciphertext) ? bytesFromBase64(object.ciphertext) : new Uint8Array(0),
      nonce: isSet(object.nonce) ? bytesFromBase64(object.nonce) : new Uint8Array(0),
      keyId: isSet(object.keyId)
        ? globalThis.String(object.keyId)
        : isSet(object.key_id)
        ? globalThis.String(object.key_id)
        : "",
      aadAmsAccountId: isSet(object.aadAmsAccountId)
        ? globalThis.String(object.aadAmsAccountId)
        : isSet(object.aad_ams_account_id)
        ? globalThis.String(object.aad_ams_account_id)
        : "",
      aadAgentId: isSet(object.aadAgentId)
        ? globalThis.String(object.aadAgentId)
        : isSet(object.aad_agent_id)
        ? globalThis.String(object.aad_agent_id)
        : "",
    };
  },

  toJSON(message: EncryptedCredential): unknown {
    const obj: any = {};
    if (message.algorithm !== 0) {
      obj.algorithm = encryptionAlgorithmToJSON(message.algorithm);
    }
    if (message.ciphertext.length !== 0) {
      obj.ciphertext = base64FromBytes(message.ciphertext);
    }
    if (message.nonce.length !== 0) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    if (message.keyId !== "") {
      obj.keyId = message.keyId;
    }
    if (message.aadAmsAccountId !== "") {
      obj.aadAmsAccountId = message.aadAmsAccountId;
    }
    if (message.aadAgentId !== "") {
      obj.aadAgentId = message.aadAgentId;
    }
    return obj;
  },

  create(base?: DeepPartial<EncryptedCredential>): EncryptedCredential {
    return EncryptedCredential.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EncryptedCredential>): EncryptedCredential {
    const message = createBaseEncryptedCredential();
    message.algorithm = object.algorithm ?? 0;
    message.ciphertext = object.ciphertext ?? new Uint8Array(0);
    message.nonce = object.nonce ?? new Uint8Array(0);
    message.keyId = object.keyId ?? "";
    message.aadAmsAccountId = object.aadAmsAccountId ?? "";
    message.aadAgentId = object.aadAgentId ?? "";
    return message;
  },
};

function createBaseUsageWindow(): UsageWindow {
  return { pct: 0, resetsAt: undefined };
}

export const UsageWindow: MessageFns<UsageWindow> = {
  encode(message: UsageWindow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pct !== 0) {
      writer.uint32(9).double(message.pct);
    }
    if (message.resetsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.resetsAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UsageWindow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsageWindow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.pct = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resetsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UsageWindow {
    return {
      pct: isSet(object.pct) ? globalThis.Number(object.pct) : 0,
      resetsAt: isSet(object.resetsAt)
        ? fromJsonTimestamp(object.resetsAt)
        : isSet(object.resets_at)
        ? fromJsonTimestamp(object.resets_at)
        : undefined,
    };
  },

  toJSON(message: UsageWindow): unknown {
    const obj: any = {};
    if (message.pct !== 0) {
      obj.pct = message.pct;
    }
    if (message.resetsAt !== undefined) {
      obj.resetsAt = message.resetsAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<UsageWindow>): UsageWindow {
    return UsageWindow.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UsageWindow>): UsageWindow {
    const message = createBaseUsageWindow();
    message.pct = object.pct ?? 0;
    message.resetsAt = object.resetsAt ?? undefined;
    return message;
  },
};

function createBaseAccountUsage(): AccountUsage {
  return {
    account: undefined,
    allocationStatus: 0,
    isCurrent: false,
    fiveHour: undefined,
    sevenDay: undefined,
    usageFetchedAt: undefined,
  };
}

export const AccountUsage: MessageFns<AccountUsage> = {
  encode(message: AccountUsage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      AccountRef.encode(message.account, writer.uint32(10).fork()).join();
    }
    if (message.allocationStatus !== 0) {
      writer.uint32(16).int32(message.allocationStatus);
    }
    if (message.isCurrent !== false) {
      writer.uint32(24).bool(message.isCurrent);
    }
    if (message.fiveHour !== undefined) {
      UsageWindow.encode(message.fiveHour, writer.uint32(34).fork()).join();
    }
    if (message.sevenDay !== undefined) {
      UsageWindow.encode(message.sevenDay, writer.uint32(42).fork()).join();
    }
    if (message.usageFetchedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.usageFetchedAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AccountUsage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountUsage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.account = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.allocationStatus = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isCurrent = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.fiveHour = UsageWindow.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sevenDay = UsageWindow.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.usageFetchedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountUsage {
    return {
      account: isSet(object.account) ? AccountRef.fromJSON(object.account) : undefined,
      allocationStatus: isSet(object.allocationStatus)
        ? allocationStatusFromJSON(object.allocationStatus)
        : isSet(object.allocation_status)
        ? allocationStatusFromJSON(object.allocation_status)
        : 0,
      isCurrent: isSet(object.isCurrent)
        ? globalThis.Boolean(object.isCurrent)
        : isSet(object.is_current)
        ? globalThis.Boolean(object.is_current)
        : false,
      fiveHour: isSet(object.fiveHour)
        ? UsageWindow.fromJSON(object.fiveHour)
        : isSet(object.five_hour)
        ? UsageWindow.fromJSON(object.five_hour)
        : undefined,
      sevenDay: isSet(object.sevenDay)
        ? UsageWindow.fromJSON(object.sevenDay)
        : isSet(object.seven_day)
        ? UsageWindow.fromJSON(object.seven_day)
        : undefined,
      usageFetchedAt: isSet(object.usageFetchedAt)
        ? fromJsonTimestamp(object.usageFetchedAt)
        : isSet(object.usage_fetched_at)
        ? fromJsonTimestamp(object.usage_fetched_at)
        : undefined,
    };
  },

  toJSON(message: AccountUsage): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = AccountRef.toJSON(message.account);
    }
    if (message.allocationStatus !== 0) {
      obj.allocationStatus = allocationStatusToJSON(message.allocationStatus);
    }
    if (message.isCurrent !== false) {
      obj.isCurrent = message.isCurrent;
    }
    if (message.fiveHour !== undefined) {
      obj.fiveHour = UsageWindow.toJSON(message.fiveHour);
    }
    if (message.sevenDay !== undefined) {
      obj.sevenDay = UsageWindow.toJSON(message.sevenDay);
    }
    if (message.usageFetchedAt !== undefined) {
      obj.usageFetchedAt = message.usageFetchedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<AccountUsage>): AccountUsage {
    return AccountUsage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AccountUsage>): AccountUsage {
    const message = createBaseAccountUsage();
    message.account = (object.account !== undefined && object.account !== null)
      ? AccountRef.fromPartial(object.account)
      : undefined;
    message.allocationStatus = object.allocationStatus ?? 0;
    message.isCurrent = object.isCurrent ?? false;
    message.fiveHour = (object.fiveHour !== undefined && object.fiveHour !== null)
      ? UsageWindow.fromPartial(object.fiveHour)
      : undefined;
    message.sevenDay = (object.sevenDay !== undefined && object.sevenDay !== null)
      ? UsageWindow.fromPartial(object.sevenDay)
      : undefined;
    message.usageFetchedAt = object.usageFetchedAt ?? undefined;
    return message;
  },
};

function createBasePoolSummary(): PoolSummary {
  return { total: 0, active: 0, eligible: 0, quarantined: 0, allExhausted: false, maxUtilizationPct: 0 };
}

export const PoolSummary: MessageFns<PoolSummary> = {
  encode(message: PoolSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total !== 0) {
      writer.uint32(8).uint32(message.total);
    }
    if (message.active !== 0) {
      writer.uint32(16).uint32(message.active);
    }
    if (message.eligible !== 0) {
      writer.uint32(24).uint32(message.eligible);
    }
    if (message.quarantined !== 0) {
      writer.uint32(32).uint32(message.quarantined);
    }
    if (message.allExhausted !== false) {
      writer.uint32(40).bool(message.allExhausted);
    }
    if (message.maxUtilizationPct !== 0) {
      writer.uint32(49).double(message.maxUtilizationPct);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PoolSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePoolSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.total = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.active = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.eligible = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.quarantined = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.allExhausted = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.maxUtilizationPct = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PoolSummary {
    return {
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      active: isSet(object.active) ? globalThis.Number(object.active) : 0,
      eligible: isSet(object.eligible) ? globalThis.Number(object.eligible) : 0,
      quarantined: isSet(object.quarantined) ? globalThis.Number(object.quarantined) : 0,
      allExhausted: isSet(object.allExhausted)
        ? globalThis.Boolean(object.allExhausted)
        : isSet(object.all_exhausted)
        ? globalThis.Boolean(object.all_exhausted)
        : false,
      maxUtilizationPct: isSet(object.maxUtilizationPct)
        ? globalThis.Number(object.maxUtilizationPct)
        : isSet(object.max_utilization_pct)
        ? globalThis.Number(object.max_utilization_pct)
        : 0,
    };
  },

  toJSON(message: PoolSummary): unknown {
    const obj: any = {};
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.active !== 0) {
      obj.active = Math.round(message.active);
    }
    if (message.eligible !== 0) {
      obj.eligible = Math.round(message.eligible);
    }
    if (message.quarantined !== 0) {
      obj.quarantined = Math.round(message.quarantined);
    }
    if (message.allExhausted !== false) {
      obj.allExhausted = message.allExhausted;
    }
    if (message.maxUtilizationPct !== 0) {
      obj.maxUtilizationPct = message.maxUtilizationPct;
    }
    return obj;
  },

  create(base?: DeepPartial<PoolSummary>): PoolSummary {
    return PoolSummary.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PoolSummary>): PoolSummary {
    const message = createBasePoolSummary();
    message.total = object.total ?? 0;
    message.active = object.active ?? 0;
    message.eligible = object.eligible ?? 0;
    message.quarantined = object.quarantined ?? 0;
    message.allExhausted = object.allExhausted ?? false;
    message.maxUtilizationPct = object.maxUtilizationPct ?? 0;
    return message;
  },
};

function createBaseAck(): Ack {
  return { accepted: false, message: "", receivedAt: undefined };
}

export const Ack: MessageFns<Ack> = {
  encode(message: Ack, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.receivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.receivedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ack {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.receivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Ack {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      receivedAt: isSet(object.receivedAt)
        ? fromJsonTimestamp(object.receivedAt)
        : isSet(object.received_at)
        ? fromJsonTimestamp(object.received_at)
        : undefined,
    };
  },

  toJSON(message: Ack): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.receivedAt !== undefined) {
      obj.receivedAt = message.receivedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Ack>): Ack {
    return Ack.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Ack>): Ack {
    const message = createBaseAck();
    message.accepted = object.accepted ?? false;
    message.message = object.message ?? "";
    message.receivedAt = object.receivedAt ?? undefined;
    return message;
  },
};

function createBaseAmsCommand(): AmsCommand {
  return {
    commandId: "",
    signature: new Uint8Array(0),
    issuedAt: undefined,
    targetAgentId: "",
    deliver: undefined,
    recall: undefined,
    setActive: undefined,
    setMode: undefined,
    switchNow: undefined,
    reqReport: undefined,
    sessionSetup: undefined,
  };
}

export const AmsCommand: MessageFns<AmsCommand> = {
  encode(message: AmsCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandId !== "") {
      writer.uint32(10).string(message.commandId);
    }
    if (message.signature.length !== 0) {
      writer.uint32(18).bytes(message.signature);
    }
    if (message.issuedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.issuedAt), writer.uint32(26).fork()).join();
    }
    if (message.targetAgentId !== "") {
      writer.uint32(34).string(message.targetAgentId);
    }
    if (message.deliver !== undefined) {
      DeliverAccount.encode(message.deliver, writer.uint32(82).fork()).join();
    }
    if (message.recall !== undefined) {
      RecallAccount.encode(message.recall, writer.uint32(90).fork()).join();
    }
    if (message.setActive !== undefined) {
      SetAccountActive.encode(message.setActive, writer.uint32(98).fork()).join();
    }
    if (message.setMode !== undefined) {
      SetSwitchMode.encode(message.setMode, writer.uint32(106).fork()).join();
    }
    if (message.switchNow !== undefined) {
      SwitchNow.encode(message.switchNow, writer.uint32(114).fork()).join();
    }
    if (message.reqReport !== undefined) {
      RequestReport.encode(message.reqReport, writer.uint32(122).fork()).join();
    }
    if (message.sessionSetup !== undefined) {
      SessionSetup.encode(message.sessionSetup, writer.uint32(130).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AmsCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmsCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.issuedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.targetAgentId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.deliver = DeliverAccount.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.recall = RecallAccount.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.setActive = SetAccountActive.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.setMode = SetSwitchMode.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.switchNow = SwitchNow.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.reqReport = RequestReport.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.sessionSetup = SessionSetup.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AmsCommand {
    return {
      commandId: isSet(object.commandId)
        ? globalThis.String(object.commandId)
        : isSet(object.command_id)
        ? globalThis.String(object.command_id)
        : "",
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      issuedAt: isSet(object.issuedAt)
        ? fromJsonTimestamp(object.issuedAt)
        : isSet(object.issued_at)
        ? fromJsonTimestamp(object.issued_at)
        : undefined,
      targetAgentId: isSet(object.targetAgentId)
        ? globalThis.String(object.targetAgentId)
        : isSet(object.target_agent_id)
        ? globalThis.String(object.target_agent_id)
        : "",
      deliver: isSet(object.deliver) ? DeliverAccount.fromJSON(object.deliver) : undefined,
      recall: isSet(object.recall) ? RecallAccount.fromJSON(object.recall) : undefined,
      setActive: isSet(object.setActive)
        ? SetAccountActive.fromJSON(object.setActive)
        : isSet(object.set_active)
        ? SetAccountActive.fromJSON(object.set_active)
        : undefined,
      setMode: isSet(object.setMode)
        ? SetSwitchMode.fromJSON(object.setMode)
        : isSet(object.set_mode)
        ? SetSwitchMode.fromJSON(object.set_mode)
        : undefined,
      switchNow: isSet(object.switchNow)
        ? SwitchNow.fromJSON(object.switchNow)
        : isSet(object.switch_now)
        ? SwitchNow.fromJSON(object.switch_now)
        : undefined,
      reqReport: isSet(object.reqReport)
        ? RequestReport.fromJSON(object.reqReport)
        : isSet(object.req_report)
        ? RequestReport.fromJSON(object.req_report)
        : undefined,
      sessionSetup: isSet(object.sessionSetup)
        ? SessionSetup.fromJSON(object.sessionSetup)
        : isSet(object.session_setup)
        ? SessionSetup.fromJSON(object.session_setup)
        : undefined,
    };
  },

  toJSON(message: AmsCommand): unknown {
    const obj: any = {};
    if (message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.issuedAt !== undefined) {
      obj.issuedAt = message.issuedAt.toISOString();
    }
    if (message.targetAgentId !== "") {
      obj.targetAgentId = message.targetAgentId;
    }
    if (message.deliver !== undefined) {
      obj.deliver = DeliverAccount.toJSON(message.deliver);
    }
    if (message.recall !== undefined) {
      obj.recall = RecallAccount.toJSON(message.recall);
    }
    if (message.setActive !== undefined) {
      obj.setActive = SetAccountActive.toJSON(message.setActive);
    }
    if (message.setMode !== undefined) {
      obj.setMode = SetSwitchMode.toJSON(message.setMode);
    }
    if (message.switchNow !== undefined) {
      obj.switchNow = SwitchNow.toJSON(message.switchNow);
    }
    if (message.reqReport !== undefined) {
      obj.reqReport = RequestReport.toJSON(message.reqReport);
    }
    if (message.sessionSetup !== undefined) {
      obj.sessionSetup = SessionSetup.toJSON(message.sessionSetup);
    }
    return obj;
  },

  create(base?: DeepPartial<AmsCommand>): AmsCommand {
    return AmsCommand.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AmsCommand>): AmsCommand {
    const message = createBaseAmsCommand();
    message.commandId = object.commandId ?? "";
    message.signature = object.signature ?? new Uint8Array(0);
    message.issuedAt = object.issuedAt ?? undefined;
    message.targetAgentId = object.targetAgentId ?? "";
    message.deliver = (object.deliver !== undefined && object.deliver !== null)
      ? DeliverAccount.fromPartial(object.deliver)
      : undefined;
    message.recall = (object.recall !== undefined && object.recall !== null)
      ? RecallAccount.fromPartial(object.recall)
      : undefined;
    message.setActive = (object.setActive !== undefined && object.setActive !== null)
      ? SetAccountActive.fromPartial(object.setActive)
      : undefined;
    message.setMode = (object.setMode !== undefined && object.setMode !== null)
      ? SetSwitchMode.fromPartial(object.setMode)
      : undefined;
    message.switchNow = (object.switchNow !== undefined && object.switchNow !== null)
      ? SwitchNow.fromPartial(object.switchNow)
      : undefined;
    message.reqReport = (object.reqReport !== undefined && object.reqReport !== null)
      ? RequestReport.fromPartial(object.reqReport)
      : undefined;
    message.sessionSetup = (object.sessionSetup !== undefined && object.sessionSetup !== null)
      ? SessionSetup.fromPartial(object.sessionSetup)
      : undefined;
    return message;
  },
};

function createBaseSessionSetup(): SessionSetup {
  return { serverCredential: "", keys: [], activeKeyId: "", revokedKeyIds: [] };
}

export const SessionSetup: MessageFns<SessionSetup> = {
  encode(message: SessionSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverCredential !== "") {
      writer.uint32(10).string(message.serverCredential);
    }
    for (const v of message.keys) {
      SessionSetup_WrappedKey.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.activeKeyId !== "") {
      writer.uint32(26).string(message.activeKeyId);
    }
    for (const v of message.revokedKeyIds) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverCredential = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.keys.push(SessionSetup_WrappedKey.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.activeKeyId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.revokedKeyIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionSetup {
    return {
      serverCredential: isSet(object.serverCredential)
        ? globalThis.String(object.serverCredential)
        : isSet(object.server_credential)
        ? globalThis.String(object.server_credential)
        : "",
      keys: globalThis.Array.isArray(object?.keys)
        ? object.keys.map((e: any) => SessionSetup_WrappedKey.fromJSON(e))
        : [],
      activeKeyId: isSet(object.activeKeyId)
        ? globalThis.String(object.activeKeyId)
        : isSet(object.active_key_id)
        ? globalThis.String(object.active_key_id)
        : "",
      revokedKeyIds: globalThis.Array.isArray(object?.revokedKeyIds)
        ? object.revokedKeyIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.revoked_key_ids)
        ? object.revoked_key_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SessionSetup): unknown {
    const obj: any = {};
    if (message.serverCredential !== "") {
      obj.serverCredential = message.serverCredential;
    }
    if (message.keys?.length) {
      obj.keys = message.keys.map((e) => SessionSetup_WrappedKey.toJSON(e));
    }
    if (message.activeKeyId !== "") {
      obj.activeKeyId = message.activeKeyId;
    }
    if (message.revokedKeyIds?.length) {
      obj.revokedKeyIds = message.revokedKeyIds;
    }
    return obj;
  },

  create(base?: DeepPartial<SessionSetup>): SessionSetup {
    return SessionSetup.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SessionSetup>): SessionSetup {
    const message = createBaseSessionSetup();
    message.serverCredential = object.serverCredential ?? "";
    message.keys = object.keys?.map((e) => SessionSetup_WrappedKey.fromPartial(e)) || [];
    message.activeKeyId = object.activeKeyId ?? "";
    message.revokedKeyIds = object.revokedKeyIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseSessionSetup_WrappedKey(): SessionSetup_WrappedKey {
  return { keyId: "", wrappedKey: new Uint8Array(0), algorithm: 0, notAfter: undefined };
}

export const SessionSetup_WrappedKey: MessageFns<SessionSetup_WrappedKey> = {
  encode(message: SessionSetup_WrappedKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.keyId !== "") {
      writer.uint32(10).string(message.keyId);
    }
    if (message.wrappedKey.length !== 0) {
      writer.uint32(18).bytes(message.wrappedKey);
    }
    if (message.algorithm !== 0) {
      writer.uint32(24).int32(message.algorithm);
    }
    if (message.notAfter !== undefined) {
      Timestamp.encode(toTimestamp(message.notAfter), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionSetup_WrappedKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionSetup_WrappedKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.wrappedKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.algorithm = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.notAfter = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionSetup_WrappedKey {
    return {
      keyId: isSet(object.keyId)
        ? globalThis.String(object.keyId)
        : isSet(object.key_id)
        ? globalThis.String(object.key_id)
        : "",
      wrappedKey: isSet(object.wrappedKey)
        ? bytesFromBase64(object.wrappedKey)
        : isSet(object.wrapped_key)
        ? bytesFromBase64(object.wrapped_key)
        : new Uint8Array(0),
      algorithm: isSet(object.algorithm) ? encryptionAlgorithmFromJSON(object.algorithm) : 0,
      notAfter: isSet(object.notAfter)
        ? fromJsonTimestamp(object.notAfter)
        : isSet(object.not_after)
        ? fromJsonTimestamp(object.not_after)
        : undefined,
    };
  },

  toJSON(message: SessionSetup_WrappedKey): unknown {
    const obj: any = {};
    if (message.keyId !== "") {
      obj.keyId = message.keyId;
    }
    if (message.wrappedKey.length !== 0) {
      obj.wrappedKey = base64FromBytes(message.wrappedKey);
    }
    if (message.algorithm !== 0) {
      obj.algorithm = encryptionAlgorithmToJSON(message.algorithm);
    }
    if (message.notAfter !== undefined) {
      obj.notAfter = message.notAfter.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<SessionSetup_WrappedKey>): SessionSetup_WrappedKey {
    return SessionSetup_WrappedKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SessionSetup_WrappedKey>): SessionSetup_WrappedKey {
    const message = createBaseSessionSetup_WrappedKey();
    message.keyId = object.keyId ?? "";
    message.wrappedKey = object.wrappedKey ?? new Uint8Array(0);
    message.algorithm = object.algorithm ?? 0;
    message.notAfter = object.notAfter ?? undefined;
    return message;
  },
};

function createBaseDeliverAccount(): DeliverAccount {
  return {
    assignmentId: "",
    account: undefined,
    credentialType: 0,
    encryptedCredential: undefined,
    desiredStatus: 0,
    organizationName: "",
    credentialExpiresAt: undefined,
  };
}

export const DeliverAccount: MessageFns<DeliverAccount> = {
  encode(message: DeliverAccount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assignmentId !== "") {
      writer.uint32(10).string(message.assignmentId);
    }
    if (message.account !== undefined) {
      AccountRef.encode(message.account, writer.uint32(18).fork()).join();
    }
    if (message.credentialType !== 0) {
      writer.uint32(24).int32(message.credentialType);
    }
    if (message.encryptedCredential !== undefined) {
      EncryptedCredential.encode(message.encryptedCredential, writer.uint32(34).fork()).join();
    }
    if (message.desiredStatus !== 0) {
      writer.uint32(40).int32(message.desiredStatus);
    }
    if (message.organizationName !== "") {
      writer.uint32(58).string(message.organizationName);
    }
    if (message.credentialExpiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.credentialExpiresAt), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeliverAccount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeliverAccount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assignmentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.account = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.credentialType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.encryptedCredential = EncryptedCredential.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.desiredStatus = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.organizationName = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.credentialExpiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeliverAccount {
    return {
      assignmentId: isSet(object.assignmentId)
        ? globalThis.String(object.assignmentId)
        : isSet(object.assignment_id)
        ? globalThis.String(object.assignment_id)
        : "",
      account: isSet(object.account) ? AccountRef.fromJSON(object.account) : undefined,
      credentialType: isSet(object.credentialType)
        ? credentialTypeFromJSON(object.credentialType)
        : isSet(object.credential_type)
        ? credentialTypeFromJSON(object.credential_type)
        : 0,
      encryptedCredential: isSet(object.encryptedCredential)
        ? EncryptedCredential.fromJSON(object.encryptedCredential)
        : isSet(object.encrypted_credential)
        ? EncryptedCredential.fromJSON(object.encrypted_credential)
        : undefined,
      desiredStatus: isSet(object.desiredStatus)
        ? allocationStatusFromJSON(object.desiredStatus)
        : isSet(object.desired_status)
        ? allocationStatusFromJSON(object.desired_status)
        : 0,
      organizationName: isSet(object.organizationName)
        ? globalThis.String(object.organizationName)
        : isSet(object.organization_name)
        ? globalThis.String(object.organization_name)
        : "",
      credentialExpiresAt: isSet(object.credentialExpiresAt)
        ? fromJsonTimestamp(object.credentialExpiresAt)
        : isSet(object.credential_expires_at)
        ? fromJsonTimestamp(object.credential_expires_at)
        : undefined,
    };
  },

  toJSON(message: DeliverAccount): unknown {
    const obj: any = {};
    if (message.assignmentId !== "") {
      obj.assignmentId = message.assignmentId;
    }
    if (message.account !== undefined) {
      obj.account = AccountRef.toJSON(message.account);
    }
    if (message.credentialType !== 0) {
      obj.credentialType = credentialTypeToJSON(message.credentialType);
    }
    if (message.encryptedCredential !== undefined) {
      obj.encryptedCredential = EncryptedCredential.toJSON(message.encryptedCredential);
    }
    if (message.desiredStatus !== 0) {
      obj.desiredStatus = allocationStatusToJSON(message.desiredStatus);
    }
    if (message.organizationName !== "") {
      obj.organizationName = message.organizationName;
    }
    if (message.credentialExpiresAt !== undefined) {
      obj.credentialExpiresAt = message.credentialExpiresAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<DeliverAccount>): DeliverAccount {
    return DeliverAccount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeliverAccount>): DeliverAccount {
    const message = createBaseDeliverAccount();
    message.assignmentId = object.assignmentId ?? "";
    message.account = (object.account !== undefined && object.account !== null)
      ? AccountRef.fromPartial(object.account)
      : undefined;
    message.credentialType = object.credentialType ?? 0;
    message.encryptedCredential = (object.encryptedCredential !== undefined && object.encryptedCredential !== null)
      ? EncryptedCredential.fromPartial(object.encryptedCredential)
      : undefined;
    message.desiredStatus = object.desiredStatus ?? 0;
    message.organizationName = object.organizationName ?? "";
    message.credentialExpiresAt = object.credentialExpiresAt ?? undefined;
    return message;
  },
};

function createBaseRecallAccount(): RecallAccount {
  return { assignmentId: "", account: undefined, purgeLocalCopy: false };
}

export const RecallAccount: MessageFns<RecallAccount> = {
  encode(message: RecallAccount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assignmentId !== "") {
      writer.uint32(10).string(message.assignmentId);
    }
    if (message.account !== undefined) {
      AccountRef.encode(message.account, writer.uint32(18).fork()).join();
    }
    if (message.purgeLocalCopy !== false) {
      writer.uint32(24).bool(message.purgeLocalCopy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecallAccount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecallAccount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assignmentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.account = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.purgeLocalCopy = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecallAccount {
    return {
      assignmentId: isSet(object.assignmentId)
        ? globalThis.String(object.assignmentId)
        : isSet(object.assignment_id)
        ? globalThis.String(object.assignment_id)
        : "",
      account: isSet(object.account) ? AccountRef.fromJSON(object.account) : undefined,
      purgeLocalCopy: isSet(object.purgeLocalCopy)
        ? globalThis.Boolean(object.purgeLocalCopy)
        : isSet(object.purge_local_copy)
        ? globalThis.Boolean(object.purge_local_copy)
        : false,
    };
  },

  toJSON(message: RecallAccount): unknown {
    const obj: any = {};
    if (message.assignmentId !== "") {
      obj.assignmentId = message.assignmentId;
    }
    if (message.account !== undefined) {
      obj.account = AccountRef.toJSON(message.account);
    }
    if (message.purgeLocalCopy !== false) {
      obj.purgeLocalCopy = message.purgeLocalCopy;
    }
    return obj;
  },

  create(base?: DeepPartial<RecallAccount>): RecallAccount {
    return RecallAccount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RecallAccount>): RecallAccount {
    const message = createBaseRecallAccount();
    message.assignmentId = object.assignmentId ?? "";
    message.account = (object.account !== undefined && object.account !== null)
      ? AccountRef.fromPartial(object.account)
      : undefined;
    message.purgeLocalCopy = object.purgeLocalCopy ?? false;
    return message;
  },
};

function createBaseSetAccountActive(): SetAccountActive {
  return { assignmentId: "", account: undefined, active: false, clearQuarantine: false };
}

export const SetAccountActive: MessageFns<SetAccountActive> = {
  encode(message: SetAccountActive, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assignmentId !== "") {
      writer.uint32(10).string(message.assignmentId);
    }
    if (message.account !== undefined) {
      AccountRef.encode(message.account, writer.uint32(18).fork()).join();
    }
    if (message.active !== false) {
      writer.uint32(24).bool(message.active);
    }
    if (message.clearQuarantine !== false) {
      writer.uint32(32).bool(message.clearQuarantine);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetAccountActive {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetAccountActive();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assignmentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.account = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.active = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.clearQuarantine = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetAccountActive {
    return {
      assignmentId: isSet(object.assignmentId)
        ? globalThis.String(object.assignmentId)
        : isSet(object.assignment_id)
        ? globalThis.String(object.assignment_id)
        : "",
      account: isSet(object.account) ? AccountRef.fromJSON(object.account) : undefined,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
      clearQuarantine: isSet(object.clearQuarantine)
        ? globalThis.Boolean(object.clearQuarantine)
        : isSet(object.clear_quarantine)
        ? globalThis.Boolean(object.clear_quarantine)
        : false,
    };
  },

  toJSON(message: SetAccountActive): unknown {
    const obj: any = {};
    if (message.assignmentId !== "") {
      obj.assignmentId = message.assignmentId;
    }
    if (message.account !== undefined) {
      obj.account = AccountRef.toJSON(message.account);
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    if (message.clearQuarantine !== false) {
      obj.clearQuarantine = message.clearQuarantine;
    }
    return obj;
  },

  create(base?: DeepPartial<SetAccountActive>): SetAccountActive {
    return SetAccountActive.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetAccountActive>): SetAccountActive {
    const message = createBaseSetAccountActive();
    message.assignmentId = object.assignmentId ?? "";
    message.account = (object.account !== undefined && object.account !== null)
      ? AccountRef.fromPartial(object.account)
      : undefined;
    message.active = object.active ?? false;
    message.clearQuarantine = object.clearQuarantine ?? false;
    return message;
  },
};

function createBaseSetSwitchMode(): SetSwitchMode {
  return { mode: 0 };
}

export const SetSwitchMode: MessageFns<SetSwitchMode> = {
  encode(message: SetSwitchMode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mode !== 0) {
      writer.uint32(8).int32(message.mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetSwitchMode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetSwitchMode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetSwitchMode {
    return { mode: isSet(object.mode) ? switchModeFromJSON(object.mode) : 0 };
  },

  toJSON(message: SetSwitchMode): unknown {
    const obj: any = {};
    if (message.mode !== 0) {
      obj.mode = switchModeToJSON(message.mode);
    }
    return obj;
  },

  create(base?: DeepPartial<SetSwitchMode>): SetSwitchMode {
    return SetSwitchMode.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetSwitchMode>): SetSwitchMode {
    const message = createBaseSetSwitchMode();
    message.mode = object.mode ?? 0;
    return message;
  },
};

function createBaseSwitchNow(): SwitchNow {
  return { account: undefined, strategy: undefined, assignmentId: "" };
}

export const SwitchNow: MessageFns<SwitchNow> = {
  encode(message: SwitchNow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      AccountRef.encode(message.account, writer.uint32(10).fork()).join();
    }
    if (message.strategy !== undefined) {
      writer.uint32(16).int32(message.strategy);
    }
    if (message.assignmentId !== "") {
      writer.uint32(26).string(message.assignmentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SwitchNow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSwitchNow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.account = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.strategy = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.assignmentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SwitchNow {
    return {
      account: isSet(object.account) ? AccountRef.fromJSON(object.account) : undefined,
      strategy: isSet(object.strategy) ? switchNow_SwitchStrategyFromJSON(object.strategy) : undefined,
      assignmentId: isSet(object.assignmentId)
        ? globalThis.String(object.assignmentId)
        : isSet(object.assignment_id)
        ? globalThis.String(object.assignment_id)
        : "",
    };
  },

  toJSON(message: SwitchNow): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = AccountRef.toJSON(message.account);
    }
    if (message.strategy !== undefined) {
      obj.strategy = switchNow_SwitchStrategyToJSON(message.strategy);
    }
    if (message.assignmentId !== "") {
      obj.assignmentId = message.assignmentId;
    }
    return obj;
  },

  create(base?: DeepPartial<SwitchNow>): SwitchNow {
    return SwitchNow.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SwitchNow>): SwitchNow {
    const message = createBaseSwitchNow();
    message.account = (object.account !== undefined && object.account !== null)
      ? AccountRef.fromPartial(object.account)
      : undefined;
    message.strategy = object.strategy ?? undefined;
    message.assignmentId = object.assignmentId ?? "";
    return message;
  },
};

function createBaseRequestReport(): RequestReport {
  return { reportType: 0, reason: "" };
}

export const RequestReport: MessageFns<RequestReport> = {
  encode(message: RequestReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reportType !== 0) {
      writer.uint32(8).int32(message.reportType);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reportType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestReport {
    return {
      reportType: isSet(object.reportType)
        ? requestReport_ReportTypeFromJSON(object.reportType)
        : isSet(object.report_type)
        ? requestReport_ReportTypeFromJSON(object.report_type)
        : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: RequestReport): unknown {
    const obj: any = {};
    if (message.reportType !== 0) {
      obj.reportType = requestReport_ReportTypeToJSON(message.reportType);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<RequestReport>): RequestReport {
    return RequestReport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RequestReport>): RequestReport {
    const message = createBaseRequestReport();
    message.reportType = object.reportType ?? 0;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseAmaMessage(): AmaMessage {
  return { register: undefined, hb: undefined, usage: undefined, ack: undefined, event: undefined };
}

export const AmaMessage: MessageFns<AmaMessage> = {
  encode(message: AmaMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.register !== undefined) {
      Register.encode(message.register, writer.uint32(82).fork()).join();
    }
    if (message.hb !== undefined) {
      Heartbeat.encode(message.hb, writer.uint32(90).fork()).join();
    }
    if (message.usage !== undefined) {
      UsageReport.encode(message.usage, writer.uint32(98).fork()).join();
    }
    if (message.ack !== undefined) {
      CommandAck.encode(message.ack, writer.uint32(106).fork()).join();
    }
    if (message.event !== undefined) {
      AccountEvent.encode(message.event, writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AmaMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmaMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.register = Register.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.hb = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.usage = UsageReport.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.ack = CommandAck.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.event = AccountEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AmaMessage {
    return {
      register: isSet(object.register) ? Register.fromJSON(object.register) : undefined,
      hb: isSet(object.hb) ? Heartbeat.fromJSON(object.hb) : undefined,
      usage: isSet(object.usage) ? UsageReport.fromJSON(object.usage) : undefined,
      ack: isSet(object.ack) ? CommandAck.fromJSON(object.ack) : undefined,
      event: isSet(object.event) ? AccountEvent.fromJSON(object.event) : undefined,
    };
  },

  toJSON(message: AmaMessage): unknown {
    const obj: any = {};
    if (message.register !== undefined) {
      obj.register = Register.toJSON(message.register);
    }
    if (message.hb !== undefined) {
      obj.hb = Heartbeat.toJSON(message.hb);
    }
    if (message.usage !== undefined) {
      obj.usage = UsageReport.toJSON(message.usage);
    }
    if (message.ack !== undefined) {
      obj.ack = CommandAck.toJSON(message.ack);
    }
    if (message.event !== undefined) {
      obj.event = AccountEvent.toJSON(message.event);
    }
    return obj;
  },

  create(base?: DeepPartial<AmaMessage>): AmaMessage {
    return AmaMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AmaMessage>): AmaMessage {
    const message = createBaseAmaMessage();
    message.register = (object.register !== undefined && object.register !== null)
      ? Register.fromPartial(object.register)
      : undefined;
    message.hb = (object.hb !== undefined && object.hb !== null) ? Heartbeat.fromPartial(object.hb) : undefined;
    message.usage = (object.usage !== undefined && object.usage !== null)
      ? UsageReport.fromPartial(object.usage)
      : undefined;
    message.ack = (object.ack !== undefined && object.ack !== null) ? CommandAck.fromPartial(object.ack) : undefined;
    message.event = (object.event !== undefined && object.event !== null)
      ? AccountEvent.fromPartial(object.event)
      : undefined;
    return message;
  },
};

function createBaseRegister(): Register {
  return {
    agentId: "",
    serverId: "",
    enrollToken: undefined,
    serverCredential: undefined,
    hostname: "",
    agentVersion: "",
    tsamxVersion: "",
    switchMode: 0,
    accounts: [],
    appliedCommandIds: [],
  };
}

export const Register: MessageFns<Register> = {
  encode(message: Register, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentId !== "") {
      writer.uint32(10).string(message.agentId);
    }
    if (message.serverId !== "") {
      writer.uint32(18).string(message.serverId);
    }
    if (message.enrollToken !== undefined) {
      writer.uint32(26).string(message.enrollToken);
    }
    if (message.serverCredential !== undefined) {
      writer.uint32(34).string(message.serverCredential);
    }
    if (message.hostname !== "") {
      writer.uint32(42).string(message.hostname);
    }
    if (message.agentVersion !== "") {
      writer.uint32(50).string(message.agentVersion);
    }
    if (message.tsamxVersion !== "") {
      writer.uint32(58).string(message.tsamxVersion);
    }
    if (message.switchMode !== 0) {
      writer.uint32(64).int32(message.switchMode);
    }
    for (const v of message.accounts) {
      AccountUsage.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.appliedCommandIds) {
      writer.uint32(82).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Register {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegister();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serverId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.enrollToken = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.serverCredential = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.agentVersion = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.tsamxVersion = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.switchMode = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.accounts.push(AccountUsage.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.appliedCommandIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Register {
    return {
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      serverId: isSet(object.serverId)
        ? globalThis.String(object.serverId)
        : isSet(object.server_id)
        ? globalThis.String(object.server_id)
        : "",
      enrollToken: isSet(object.enrollToken)
        ? globalThis.String(object.enrollToken)
        : isSet(object.enroll_token)
        ? globalThis.String(object.enroll_token)
        : undefined,
      serverCredential: isSet(object.serverCredential)
        ? globalThis.String(object.serverCredential)
        : isSet(object.server_credential)
        ? globalThis.String(object.server_credential)
        : undefined,
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      agentVersion: isSet(object.agentVersion)
        ? globalThis.String(object.agentVersion)
        : isSet(object.agent_version)
        ? globalThis.String(object.agent_version)
        : "",
      tsamxVersion: isSet(object.tsamxVersion)
        ? globalThis.String(object.tsamxVersion)
        : isSet(object.tsamx_version)
        ? globalThis.String(object.tsamx_version)
        : "",
      switchMode: isSet(object.switchMode)
        ? switchModeFromJSON(object.switchMode)
        : isSet(object.switch_mode)
        ? switchModeFromJSON(object.switch_mode)
        : 0,
      accounts: globalThis.Array.isArray(object?.accounts)
        ? object.accounts.map((e: any) => AccountUsage.fromJSON(e))
        : [],
      appliedCommandIds: globalThis.Array.isArray(object?.appliedCommandIds)
        ? object.appliedCommandIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.applied_command_ids)
        ? object.applied_command_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: Register): unknown {
    const obj: any = {};
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.serverId !== "") {
      obj.serverId = message.serverId;
    }
    if (message.enrollToken !== undefined) {
      obj.enrollToken = message.enrollToken;
    }
    if (message.serverCredential !== undefined) {
      obj.serverCredential = message.serverCredential;
    }
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.agentVersion !== "") {
      obj.agentVersion = message.agentVersion;
    }
    if (message.tsamxVersion !== "") {
      obj.tsamxVersion = message.tsamxVersion;
    }
    if (message.switchMode !== 0) {
      obj.switchMode = switchModeToJSON(message.switchMode);
    }
    if (message.accounts?.length) {
      obj.accounts = message.accounts.map((e) => AccountUsage.toJSON(e));
    }
    if (message.appliedCommandIds?.length) {
      obj.appliedCommandIds = message.appliedCommandIds;
    }
    return obj;
  },

  create(base?: DeepPartial<Register>): Register {
    return Register.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Register>): Register {
    const message = createBaseRegister();
    message.agentId = object.agentId ?? "";
    message.serverId = object.serverId ?? "";
    message.enrollToken = object.enrollToken ?? undefined;
    message.serverCredential = object.serverCredential ?? undefined;
    message.hostname = object.hostname ?? "";
    message.agentVersion = object.agentVersion ?? "";
    message.tsamxVersion = object.tsamxVersion ?? "";
    message.switchMode = object.switchMode ?? 0;
    message.accounts = object.accounts?.map((e) => AccountUsage.fromPartial(e)) || [];
    message.appliedCommandIds = object.appliedCommandIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseHeartbeat(): Heartbeat {
  return {
    agentId: "",
    sentAt: undefined,
    activeAccount: undefined,
    switchMode: 0,
    tsamxHealthy: false,
    outboxDepth: 0,
  };
}

export const Heartbeat: MessageFns<Heartbeat> = {
  encode(message: Heartbeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentId !== "") {
      writer.uint32(10).string(message.agentId);
    }
    if (message.sentAt !== undefined) {
      Timestamp.encode(toTimestamp(message.sentAt), writer.uint32(18).fork()).join();
    }
    if (message.activeAccount !== undefined) {
      AccountRef.encode(message.activeAccount, writer.uint32(26).fork()).join();
    }
    if (message.switchMode !== 0) {
      writer.uint32(32).int32(message.switchMode);
    }
    if (message.tsamxHealthy !== false) {
      writer.uint32(40).bool(message.tsamxHealthy);
    }
    if (message.outboxDepth !== 0) {
      writer.uint32(48).uint32(message.outboxDepth);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Heartbeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sentAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.activeAccount = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.switchMode = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tsamxHealthy = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.outboxDepth = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Heartbeat {
    return {
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      sentAt: isSet(object.sentAt)
        ? fromJsonTimestamp(object.sentAt)
        : isSet(object.sent_at)
        ? fromJsonTimestamp(object.sent_at)
        : undefined,
      activeAccount: isSet(object.activeAccount)
        ? AccountRef.fromJSON(object.activeAccount)
        : isSet(object.active_account)
        ? AccountRef.fromJSON(object.active_account)
        : undefined,
      switchMode: isSet(object.switchMode)
        ? switchModeFromJSON(object.switchMode)
        : isSet(object.switch_mode)
        ? switchModeFromJSON(object.switch_mode)
        : 0,
      tsamxHealthy: isSet(object.tsamxHealthy)
        ? globalThis.Boolean(object.tsamxHealthy)
        : isSet(object.tsamx_healthy)
        ? globalThis.Boolean(object.tsamx_healthy)
        : false,
      outboxDepth: isSet(object.outboxDepth)
        ? globalThis.Number(object.outboxDepth)
        : isSet(object.outbox_depth)
        ? globalThis.Number(object.outbox_depth)
        : 0,
    };
  },

  toJSON(message: Heartbeat): unknown {
    const obj: any = {};
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.sentAt !== undefined) {
      obj.sentAt = message.sentAt.toISOString();
    }
    if (message.activeAccount !== undefined) {
      obj.activeAccount = AccountRef.toJSON(message.activeAccount);
    }
    if (message.switchMode !== 0) {
      obj.switchMode = switchModeToJSON(message.switchMode);
    }
    if (message.tsamxHealthy !== false) {
      obj.tsamxHealthy = message.tsamxHealthy;
    }
    if (message.outboxDepth !== 0) {
      obj.outboxDepth = Math.round(message.outboxDepth);
    }
    return obj;
  },

  create(base?: DeepPartial<Heartbeat>): Heartbeat {
    return Heartbeat.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Heartbeat>): Heartbeat {
    const message = createBaseHeartbeat();
    message.agentId = object.agentId ?? "";
    message.sentAt = object.sentAt ?? undefined;
    message.activeAccount = (object.activeAccount !== undefined && object.activeAccount !== null)
      ? AccountRef.fromPartial(object.activeAccount)
      : undefined;
    message.switchMode = object.switchMode ?? 0;
    message.tsamxHealthy = object.tsamxHealthy ?? false;
    message.outboxDepth = object.outboxDepth ?? 0;
    return message;
  },
};

function createBaseUsageReport(): UsageReport {
  return {
    schemaVersion: 0,
    agentId: "",
    generatedAt: undefined,
    trigger: 0,
    activeAccount: undefined,
    poolSummary: undefined,
    accounts: [],
    inResponseToCommandId: "",
  };
}

export const UsageReport: MessageFns<UsageReport> = {
  encode(message: UsageReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.schemaVersion !== 0) {
      writer.uint32(8).uint32(message.schemaVersion);
    }
    if (message.agentId !== "") {
      writer.uint32(18).string(message.agentId);
    }
    if (message.generatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.generatedAt), writer.uint32(26).fork()).join();
    }
    if (message.trigger !== 0) {
      writer.uint32(32).int32(message.trigger);
    }
    if (message.activeAccount !== undefined) {
      AccountRef.encode(message.activeAccount, writer.uint32(42).fork()).join();
    }
    if (message.poolSummary !== undefined) {
      PoolSummary.encode(message.poolSummary, writer.uint32(50).fork()).join();
    }
    for (const v of message.accounts) {
      AccountUsage.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.inResponseToCommandId !== "") {
      writer.uint32(66).string(message.inResponseToCommandId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UsageReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsageReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.schemaVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.generatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.trigger = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.activeAccount = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.poolSummary = PoolSummary.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.accounts.push(AccountUsage.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.inResponseToCommandId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UsageReport {
    return {
      schemaVersion: isSet(object.schemaVersion)
        ? globalThis.Number(object.schemaVersion)
        : isSet(object.schema_version)
        ? globalThis.Number(object.schema_version)
        : 0,
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      generatedAt: isSet(object.generatedAt)
        ? fromJsonTimestamp(object.generatedAt)
        : isSet(object.generated_at)
        ? fromJsonTimestamp(object.generated_at)
        : undefined,
      trigger: isSet(object.trigger) ? usageReport_TriggerFromJSON(object.trigger) : 0,
      activeAccount: isSet(object.activeAccount)
        ? AccountRef.fromJSON(object.activeAccount)
        : isSet(object.active_account)
        ? AccountRef.fromJSON(object.active_account)
        : undefined,
      poolSummary: isSet(object.poolSummary)
        ? PoolSummary.fromJSON(object.poolSummary)
        : isSet(object.pool_summary)
        ? PoolSummary.fromJSON(object.pool_summary)
        : undefined,
      accounts: globalThis.Array.isArray(object?.accounts)
        ? object.accounts.map((e: any) => AccountUsage.fromJSON(e))
        : [],
      inResponseToCommandId: isSet(object.inResponseToCommandId)
        ? globalThis.String(object.inResponseToCommandId)
        : isSet(object.in_response_to_command_id)
        ? globalThis.String(object.in_response_to_command_id)
        : "",
    };
  },

  toJSON(message: UsageReport): unknown {
    const obj: any = {};
    if (message.schemaVersion !== 0) {
      obj.schemaVersion = Math.round(message.schemaVersion);
    }
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.generatedAt !== undefined) {
      obj.generatedAt = message.generatedAt.toISOString();
    }
    if (message.trigger !== 0) {
      obj.trigger = usageReport_TriggerToJSON(message.trigger);
    }
    if (message.activeAccount !== undefined) {
      obj.activeAccount = AccountRef.toJSON(message.activeAccount);
    }
    if (message.poolSummary !== undefined) {
      obj.poolSummary = PoolSummary.toJSON(message.poolSummary);
    }
    if (message.accounts?.length) {
      obj.accounts = message.accounts.map((e) => AccountUsage.toJSON(e));
    }
    if (message.inResponseToCommandId !== "") {
      obj.inResponseToCommandId = message.inResponseToCommandId;
    }
    return obj;
  },

  create(base?: DeepPartial<UsageReport>): UsageReport {
    return UsageReport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UsageReport>): UsageReport {
    const message = createBaseUsageReport();
    message.schemaVersion = object.schemaVersion ?? 0;
    message.agentId = object.agentId ?? "";
    message.generatedAt = object.generatedAt ?? undefined;
    message.trigger = object.trigger ?? 0;
    message.activeAccount = (object.activeAccount !== undefined && object.activeAccount !== null)
      ? AccountRef.fromPartial(object.activeAccount)
      : undefined;
    message.poolSummary = (object.poolSummary !== undefined && object.poolSummary !== null)
      ? PoolSummary.fromPartial(object.poolSummary)
      : undefined;
    message.accounts = object.accounts?.map((e) => AccountUsage.fromPartial(e)) || [];
    message.inResponseToCommandId = object.inResponseToCommandId ?? "";
    return message;
  },
};

function createBaseReportEnvelope(): ReportEnvelope {
  return { serverCredential: "", report: undefined };
}

export const ReportEnvelope: MessageFns<ReportEnvelope> = {
  encode(message: ReportEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverCredential !== "") {
      writer.uint32(10).string(message.serverCredential);
    }
    if (message.report !== undefined) {
      UsageReport.encode(message.report, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverCredential = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.report = UsageReport.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEnvelope {
    return {
      serverCredential: isSet(object.serverCredential)
        ? globalThis.String(object.serverCredential)
        : isSet(object.server_credential)
        ? globalThis.String(object.server_credential)
        : "",
      report: isSet(object.report) ? UsageReport.fromJSON(object.report) : undefined,
    };
  },

  toJSON(message: ReportEnvelope): unknown {
    const obj: any = {};
    if (message.serverCredential !== "") {
      obj.serverCredential = message.serverCredential;
    }
    if (message.report !== undefined) {
      obj.report = UsageReport.toJSON(message.report);
    }
    return obj;
  },

  create(base?: DeepPartial<ReportEnvelope>): ReportEnvelope {
    return ReportEnvelope.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReportEnvelope>): ReportEnvelope {
    const message = createBaseReportEnvelope();
    message.serverCredential = object.serverCredential ?? "";
    message.report = (object.report !== undefined && object.report !== null)
      ? UsageReport.fromPartial(object.report)
      : undefined;
    return message;
  },
};

function createBaseCommandAck(): CommandAck {
  return {
    commandId: "",
    agentId: "",
    observedAt: undefined,
    convergence: 0,
    accountState: undefined,
    switchMode: 0,
    poolSummary: undefined,
    detail: "",
    errorCode: "",
  };
}

export const CommandAck: MessageFns<CommandAck> = {
  encode(message: CommandAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandId !== "") {
      writer.uint32(10).string(message.commandId);
    }
    if (message.agentId !== "") {
      writer.uint32(18).string(message.agentId);
    }
    if (message.observedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.observedAt), writer.uint32(26).fork()).join();
    }
    if (message.convergence !== 0) {
      writer.uint32(32).int32(message.convergence);
    }
    if (message.accountState !== undefined) {
      AccountUsage.encode(message.accountState, writer.uint32(42).fork()).join();
    }
    if (message.switchMode !== 0) {
      writer.uint32(48).int32(message.switchMode);
    }
    if (message.poolSummary !== undefined) {
      PoolSummary.encode(message.poolSummary, writer.uint32(58).fork()).join();
    }
    if (message.detail !== "") {
      writer.uint32(66).string(message.detail);
    }
    if (message.errorCode !== "") {
      writer.uint32(74).string(message.errorCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.observedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.convergence = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.accountState = AccountUsage.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.switchMode = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.poolSummary = PoolSummary.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.detail = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.errorCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandAck {
    return {
      commandId: isSet(object.commandId)
        ? globalThis.String(object.commandId)
        : isSet(object.command_id)
        ? globalThis.String(object.command_id)
        : "",
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      observedAt: isSet(object.observedAt)
        ? fromJsonTimestamp(object.observedAt)
        : isSet(object.observed_at)
        ? fromJsonTimestamp(object.observed_at)
        : undefined,
      convergence: isSet(object.convergence) ? commandAck_ConvergenceFromJSON(object.convergence) : 0,
      accountState: isSet(object.accountState)
        ? AccountUsage.fromJSON(object.accountState)
        : isSet(object.account_state)
        ? AccountUsage.fromJSON(object.account_state)
        : undefined,
      switchMode: isSet(object.switchMode)
        ? switchModeFromJSON(object.switchMode)
        : isSet(object.switch_mode)
        ? switchModeFromJSON(object.switch_mode)
        : 0,
      poolSummary: isSet(object.poolSummary)
        ? PoolSummary.fromJSON(object.poolSummary)
        : isSet(object.pool_summary)
        ? PoolSummary.fromJSON(object.pool_summary)
        : undefined,
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
      errorCode: isSet(object.errorCode)
        ? globalThis.String(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.String(object.error_code)
        : "",
    };
  },

  toJSON(message: CommandAck): unknown {
    const obj: any = {};
    if (message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.observedAt !== undefined) {
      obj.observedAt = message.observedAt.toISOString();
    }
    if (message.convergence !== 0) {
      obj.convergence = commandAck_ConvergenceToJSON(message.convergence);
    }
    if (message.accountState !== undefined) {
      obj.accountState = AccountUsage.toJSON(message.accountState);
    }
    if (message.switchMode !== 0) {
      obj.switchMode = switchModeToJSON(message.switchMode);
    }
    if (message.poolSummary !== undefined) {
      obj.poolSummary = PoolSummary.toJSON(message.poolSummary);
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    if (message.errorCode !== "") {
      obj.errorCode = message.errorCode;
    }
    return obj;
  },

  create(base?: DeepPartial<CommandAck>): CommandAck {
    return CommandAck.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CommandAck>): CommandAck {
    const message = createBaseCommandAck();
    message.commandId = object.commandId ?? "";
    message.agentId = object.agentId ?? "";
    message.observedAt = object.observedAt ?? undefined;
    message.convergence = object.convergence ?? 0;
    message.accountState = (object.accountState !== undefined && object.accountState !== null)
      ? AccountUsage.fromPartial(object.accountState)
      : undefined;
    message.switchMode = object.switchMode ?? 0;
    message.poolSummary = (object.poolSummary !== undefined && object.poolSummary !== null)
      ? PoolSummary.fromPartial(object.poolSummary)
      : undefined;
    message.detail = object.detail ?? "";
    message.errorCode = object.errorCode ?? "";
    return message;
  },
};

function createBaseAccountEvent(): AccountEvent {
  return {
    schemaVersion: 0,
    agentId: "",
    eventId: "",
    occurredAt: undefined,
    kind: 0,
    trigger: 0,
    from: undefined,
    to: undefined,
    poolSummary: undefined,
    detail: "",
  };
}

export const AccountEvent: MessageFns<AccountEvent> = {
  encode(message: AccountEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.schemaVersion !== 0) {
      writer.uint32(8).uint32(message.schemaVersion);
    }
    if (message.agentId !== "") {
      writer.uint32(18).string(message.agentId);
    }
    if (message.eventId !== "") {
      writer.uint32(26).string(message.eventId);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(34).fork()).join();
    }
    if (message.kind !== 0) {
      writer.uint32(40).int32(message.kind);
    }
    if (message.trigger !== 0) {
      writer.uint32(48).int32(message.trigger);
    }
    if (message.from !== undefined) {
      AccountRef.encode(message.from, writer.uint32(58).fork()).join();
    }
    if (message.to !== undefined) {
      AccountRef.encode(message.to, writer.uint32(66).fork()).join();
    }
    if (message.poolSummary !== undefined) {
      PoolSummary.encode(message.poolSummary, writer.uint32(74).fork()).join();
    }
    if (message.detail !== "") {
      writer.uint32(82).string(message.detail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AccountEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.schemaVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.trigger = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.from = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.to = AccountRef.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.poolSummary = PoolSummary.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.detail = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountEvent {
    return {
      schemaVersion: isSet(object.schemaVersion)
        ? globalThis.Number(object.schemaVersion)
        : isSet(object.schema_version)
        ? globalThis.Number(object.schema_version)
        : 0,
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      occurredAt: isSet(object.occurredAt)
        ? fromJsonTimestamp(object.occurredAt)
        : isSet(object.occurred_at)
        ? fromJsonTimestamp(object.occurred_at)
        : undefined,
      kind: isSet(object.kind) ? accountEvent_KindFromJSON(object.kind) : 0,
      trigger: isSet(object.trigger) ? accountEvent_TriggerFromJSON(object.trigger) : 0,
      from: isSet(object.from) ? AccountRef.fromJSON(object.from) : undefined,
      to: isSet(object.to) ? AccountRef.fromJSON(object.to) : undefined,
      poolSummary: isSet(object.poolSummary)
        ? PoolSummary.fromJSON(object.poolSummary)
        : isSet(object.pool_summary)
        ? PoolSummary.fromJSON(object.pool_summary)
        : undefined,
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
    };
  },

  toJSON(message: AccountEvent): unknown {
    const obj: any = {};
    if (message.schemaVersion !== 0) {
      obj.schemaVersion = Math.round(message.schemaVersion);
    }
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.occurredAt !== undefined) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.kind !== 0) {
      obj.kind = accountEvent_KindToJSON(message.kind);
    }
    if (message.trigger !== 0) {
      obj.trigger = accountEvent_TriggerToJSON(message.trigger);
    }
    if (message.from !== undefined) {
      obj.from = AccountRef.toJSON(message.from);
    }
    if (message.to !== undefined) {
      obj.to = AccountRef.toJSON(message.to);
    }
    if (message.poolSummary !== undefined) {
      obj.poolSummary = PoolSummary.toJSON(message.poolSummary);
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    return obj;
  },

  create(base?: DeepPartial<AccountEvent>): AccountEvent {
    return AccountEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AccountEvent>): AccountEvent {
    const message = createBaseAccountEvent();
    message.schemaVersion = object.schemaVersion ?? 0;
    message.agentId = object.agentId ?? "";
    message.eventId = object.eventId ?? "";
    message.occurredAt = object.occurredAt ?? undefined;
    message.kind = object.kind ?? 0;
    message.trigger = object.trigger ?? 0;
    message.from = (object.from !== undefined && object.from !== null)
      ? AccountRef.fromPartial(object.from)
      : undefined;
    message.to = (object.to !== undefined && object.to !== null) ? AccountRef.fromPartial(object.to) : undefined;
    message.poolSummary = (object.poolSummary !== undefined && object.poolSummary !== null)
      ? PoolSummary.fromPartial(object.poolSummary)
      : undefined;
    message.detail = object.detail ?? "";
    return message;
  },
};

/**
 * AMA dials AMS outbound over TLS/443 (D9). AMS never dials AMA, so no inbound
 * port is required on the customer network.
 */
export type AmxControlPlaneDefinition = typeof AmxControlPlaneDefinition;
export const AmxControlPlaneDefinition = {
  name: "AmxControlPlane",
  fullName: "amx.v1.AmxControlPlane",
  methods: {
    /**
     * Single long-lived stream opened by AMA.
     *   upstream   (AmaMessage): register / heartbeat / usage / ack / event
     *   downstream (AmsCommand): deliver / recall / activate / mode / switch / report
     * The first upstream message MUST be Register; AMS closes the stream otherwise.
     */
    session: {
      name: "Session",
      requestType: AmaMessage as typeof AmaMessage,
      requestStream: true,
      responseType: AmsCommand as typeof AmsCommand,
      responseStream: true,
      options: {},
    },
    /**
     * Fallback for environments where the bidi stream is unavailable
     * (proxy that buffers streams, restricted egress). Report-only: commands
     * cannot be delivered over this path.
     *
     * Takes an envelope rather than a bare UsageReport so the caller's credential
     * stays outside the report body: the report is persisted verbatim as
     * usage_snapshots.payload (§5.1) and must never carry a secret.
     */
    reportUsage: {
      name: "ReportUsage",
      requestType: ReportEnvelope as typeof ReportEnvelope,
      requestStream: false,
      responseType: Ack as typeof Ack,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
